server {
  listen   80;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
    root   /var/www/nginx-default/<%= node['nginx']['rails_app'] %>/current/public;
    passenger_enabled on;
  }
}
